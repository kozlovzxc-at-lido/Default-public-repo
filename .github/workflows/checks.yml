name: Checks

on:
  pull_request:
jobs:
  checks:
    runs-on: ubuntu-latest
    name: Checks
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Hello world
        run: echo hello world

      - name: Heck
        run: |
        git clone git@github.com:kozlovzxc-at-lido/Default-public-repo.git
        ls Default-public-repo
