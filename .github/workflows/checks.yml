name: Checks

on:
  pull_request:
jobs:
  checks:
    runs-on: ubuntu-latest
    name: Checks
    environment: test
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Hello world
        run: echo hello world

      - name: Other echo 
        run: echo hello ${{ secrets.SECRET }} world

      - name: Other other echo
        run: echo hello ${{ secrets.ESECRET }} world
