name: Checks

on:
  pull_request:
jobs:
  checks:
    runs-on: ubuntu-latest
    name: Checks
    environment: test
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Hello world
        run: echo hello world

      - name: Repo secret 
        run: echo hello ${{ secrets.SECRET }} world

      - name: Env secret
        run: echo hello ${{ secrets.ESECRET }} world
